# Textarea that Autocompletes Usernames

This project is a React component that provides autocomplete functionality for usernames in a textarea. It allows users to type an "@" symbol and then displays a list of suggestions for usernames to choose from.